<!--if user signs in
  direct them to the user page
  else
  direct them to guest page
end -->


<% if !user_signed_in? %>
  <%= render 'layouts/guest_nav' %>
<% else %>
  <%= render 'layouts/user_nav' %>  
<% end %>
